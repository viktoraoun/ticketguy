---
interface Props {
	title: string;
	imageUrl: string;
    alt: string;
}

const { title, imageUrl, alt } = Astro.props;
---

<article class="flex flex-col cursor-pointer my-4 md:basis-[48%]">
    <img class="shadow-lg self-start" src=`${imageUrl}` alt=`${alt}` />
    <h3 class="text-2xl">{title}</h3>
</article>
